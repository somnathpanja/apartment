---
deployment:
 tasks:
 - export DEPLOYPATH=/home/alpinin/apt/
 - /bin/cp package.json $DEPLOYPATH
 - /bin/cp controller.js $DEPLOYPATH
 - /bin/cp -R common $DEPLOYPATH
 - /bin/cp -R public $DEPLOYPATH
